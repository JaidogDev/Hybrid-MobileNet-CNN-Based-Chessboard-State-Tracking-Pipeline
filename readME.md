python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

py -3.12 -m venv .venv312
.\.venv312\Scripts\Activate.ps1
pip install -r requirements.txt

How to run
1.set up venv
2.run all cell start with notebook1 > notebook4 
3.for notebook4 just run cell1, cell2 then, test with cell 3.1 or 3.2
